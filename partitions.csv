# ESP-IDF Partition Table
#
# No www partition: the built frontend is packed into one blob and linked into
# the app image (main/strux/WebAssets/), so a firmware image is the whole product
# and the space the FAT image used is back in the two OTA slots. The slots fill
# the 4 MB flash exactly.
#
# Name,     Type,   SubType,    Offset,     Size,       Flags
nvs,        data,   nvs,        0x9000,     0x6000,
otadata,    data,   ota,        0xf000,     0x2000,
phy_init,   data,   phy,        0x11000,    0x1000,
ota_0,      app,    ota_0,      0x20000,    0x1F0000,
ota_1,      app,    ota_1,      0x210000,   0x1F0000,
